# PostgreSQL Database Connection String
# Format: postgresql://username:password@host:port/database
# Example for local development: postgresql://postgres:password@localhost:5432/entipedia
# Example for production: Use your PostgreSQL provider's connection string
# This file should be copied to .env for local development and never committed to version control.
# For production (e.g., Netlify), configure this variable in the deployment environment.
# Append `?ssl=true` when your provider mandates TLS (postgres.js also accepts `?sslmode=require`).
DATABASE_URL=postgresql://postgres:password@localhost:5432/entipedia
