# Database Configuration
# PostgreSQL connection string for Drizzle ORM
# Format: postgresql://username:password@host:port/database
# For local development: postgresql://postgres:postgres@localhost:5432/entipedia
# For production: Use the connection string provided by your hosting provider (e.g., Railway)
# Security: Never commit the actual .env file with real credentials
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/entipedia

# Session Configuration
# Secret key used for signing and encrypting session cookies
# Generate a secure random string (e.g., using: openssl rand -hex 32)
# For production: Use a strong, randomly generated secret stored in your hosting provider's environment variables
# Security: Never commit the actual .env file with the real secret
SESSION_SECRET=your_session_secret_here_change_in_production

# Cloudflare R2 (S3-Compatible) Storage Configuration
# Required for file uploads and storage
# 
# R2_ACCOUNT_ID: Your Cloudflare account ID
#   - Find it in your Cloudflare dashboard under "Account ID" in the right sidebar
#   - Example: abc123def456ghi789
#
# R2_ACCESS_KEY_ID: Access key ID for R2 API access
#   - Create in Cloudflare dashboard: R2 > Manage R2 API Tokens > Create API Token
#   - Grant "Object Read & Write" permissions
#   - Example: abc123def456ghi789jkl012mno345
#
# R2_SECRET_ACCESS_KEY: Secret access key for R2 API access
#   - Created together with R2_ACCESS_KEY_ID
#   - Keep this secret and never commit it to version control
#   - Example: abc123def456ghi789jkl012mno345pqr678stu901vwx234
#
# R2_BUCKET_NAME: Name of your R2 bucket
#   - Create a bucket in Cloudflare dashboard: R2 > Create bucket
#   - Use a descriptive name (e.g., entipedia-files)
#   - Example: entipedia-files
#
# Security: Never commit the actual .env file with real credentials
R2_ACCOUNT_ID=your_r2_account_id_here
R2_ACCESS_KEY_ID=your_r2_access_key_id_here
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key_here
R2_BUCKET_NAME=your_r2_bucket_name_here

# Resend Email Service Configuration
# Required for sending verification emails
#
# RESEND_API_KEY: Your Resend API key
#   - Sign up at https://resend.com
#   - Navigate to API Keys section in your dashboard
#   - Create a new API key
#   - For development: Use the test API key provided by Resend
#   - For production: Use a production API key and configure a custom sending domain
#   - Format: re_123456789_your_api_key_here
#   - Security: Never commit the actual .env file with the real API key
#   - Set this in your hosting provider's environment variables (e.g., Railway)
RESEND_API_KEY=re_123456789_your_api_key_here

# Application URL Configuration
# Base URL of the application, used for generating verification links and other absolute URLs
#
# APP_URL: The base URL where your application is hosted
#   - For local development: http://localhost:5000 (matching the Vite dev server port)
#   - For production: The actual production URL (e.g., https://entipedia.com or Railway-provided URL)
#   - Must NOT include a trailing slash
#   - This URL is used to construct verification links in emails
#   - Example: http://localhost:5000 or https://entipedia.com
APP_URL=http://localhost:5000

# Email Verification Behavior
# Optional flag to bypass verification email sending and auto-verify users.
# Enable (set to true) only in local development; leave unset or false in staging/production.
SKIP_EMAIL_VERIFICATION=false
